{
  "installCommand": "if [ -d frontend ]; then npm install && npm --prefix frontend install; else npm install; fi",
  "buildCommand": "if [ -d frontend ]; then npm --prefix frontend run build && rm -rf dist && cp -r frontend/dist dist; else npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
